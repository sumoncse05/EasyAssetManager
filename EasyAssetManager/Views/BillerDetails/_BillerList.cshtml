@model IEnumerable<EasyAssetManagerCore.Models.EntityModel.Biller>
@{
    Layout = null;
}
@if (Model != null && Model.Any())
{
    <table class="table table-striped table-bordered table-hover dataTables-example" id="tb-activeBiller" width="100%">
        <thead>
            <tr>
                <th>Biller ID</th>
                <th>Biller Name</th>
                <th>Status</th>
                <th>Biller Account</th>
                <th>Ins By</th>
                <th>Ins Date</th>
                <th>Edit</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var v in Model)
            {
                <tr>
                    <td>@v.biller_id</td>
                    <td>@v.biller_desc</td>
                    <td>
                        @{
                            if (v.status == "Y")
                            { <span>Yes</span> }
                            else
                            { <span>No</span> }
                        }
                    </td>
                    <td>@v.cust_ac_no </td>
                    <td>@v.ins_by</td>
                    <td>@v.ins_date</td>
                    @if (v.auth_date == null)
                    {
                        <td>
                            <a href="/BillerDetails/Index?biller_id=@v.biller_id" class="btn btn-success gridButton" data-id="@v.biller_id"><i class="fa fa-edit"></i></a>

                        </td>
                    }
                    else
                    {
                        <td></td>
                    }
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No record found.</p>
}

<script type="text/javascript">
    $(document).ready(function () {
        ApplicationCommon.makePagination("tb-activeBiller");

        var rowCount = $('#tb-activeBiller >tbody >tr').length;
        if (rowCount > 0) {
            $("#sidebarCollapse").addClass('fa-chevron-down').removeClass('fa-chevron-up');
            $("#divsidebarCollapse").addClass('border-bottom');
            $('#sidebarContent').css('display', 'none');
        }
    });

</script>

